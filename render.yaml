services:
  - type: web
    name: outlook-email-api
    runtime: node
    plan: starter
    buildCommand: npm install && npx playwright install --with-deps firefox
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: HEADLESS
        value: true
      - key: LOG_LEVEL
        value: info